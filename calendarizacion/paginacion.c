#include "list.c"
#include <math.h>
void paginacion(char* arg[], p* head){
    int tam= atoi(arg[2]);
    int cant= atoi(arg[1]);
    int cantceldas= tam/cant;
    int tamcelda= sqrt(cantceldas);
    p* matriz [tamcelda][tamcelda];


}